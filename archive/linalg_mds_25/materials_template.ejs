```{=html}
<% for (const item of items) { %>
    <h3>Занятие <%= item.number %></h3>
    <p>
        <% 
            var links = [];
            if(item.lectures) { links.push('<a href="' + item.lectures + '">📄 Презентация</a>'); }
            if(item.lectures_en) { links.push('<a href="' + item.lectures_en + '">📄 Presentation</a>'); }
            if(item.notes) { links.push('<a href="' + item.notes + '">📝 Заметки</a>'); }
            if(item.notes_1class) { links.push('<a href="' + item.notes_1class + '">📝 Заметки 1 поток</a>'); }
            if(item.notes_2class) { links.push('<a href="' + item.notes_2class + '">📝 Заметки 2 поток</a>'); }
            if(item.assignments) { links.push('<a href="' + item.assignments + '">📝 Список заданий</a>'); }
            if(item.download) { links.push('<a href="' + item.download + '">💿 Скачать</a>'); }
            if(item.mind_map) { links.push('<a href="' + item.mind_map + '">🗺️ Mind Map</a>'); }
            print(links.join(' • '));
        %>
    </p>
    <p>
        <%= item.title %>
    </p>
<% } %>
```
