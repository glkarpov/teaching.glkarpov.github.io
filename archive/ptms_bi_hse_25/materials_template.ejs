```{=html}
<% for (const item of items) { %>
    <h3>Занятие <%= item.number %></h3>
    <p>
        <% 
            var links = [];
            if(item.materials) { links.push('<a href="' + item.materials + '">📄 Презентация</a>'); }
            if(item.longread) { links.push('<a href="' + item.longread + '">📜 Лонгрид</a>'); }
            if(item.notes) { links.push('<a href="' + item.notes + '">📝 Заметки</a>'); }
            if(item.seminar) { links.push('<a href="' + item.seminar + '">👷‍♂️ Seminar</a>'); }
            if(item.youtube) { links.push('<a href="' + item.youtube + '">▶️ Youtube</a>'); }
            if(item.download) { links.push('<a href="' + item.download + '">💿 Скачать</a>'); }
            print(links.join(' • '));
        %>
    </p>
    <p>
        <% 
            var linksCu = [];
            if(item.materials_cu) { linksCu.push('<a href="' + item.materials_cu + '">📄 Презентация (CU)</a>'); }
            if(item.seminar_cu) { linksCu.push('<a href="' + item.seminar_cu + '">👷‍♂️ Seminar (CU)</a>'); }
            if (linksCu.length) { print(linksCu.join(' • ')); }
        %>
    </p>
    <p><%= item.title %></p>
<% } %>
```